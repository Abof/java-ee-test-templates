CREATE TABLE AUTHOR(ID INT PRIMARY KEY, FORNAME VARCHAR(255), SURNAME VARCHAR(255));
CREATE TABLE BOOK(ID INT PRIMARY KEY, TITLE VARCHAR(255) NOT NULL, AUTHOR_ID INT NOT NULL, FOREIGN KEY(AUTHOR_ID) REFERENCES AUTHOR(ID));